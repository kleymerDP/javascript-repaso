/*
Dado un array de números, podemos obtener un nuevo array con los dobles de cada números
invocando al método map() sobre el array original.
*/

const numeros = [3, 10, 20, 50];
const dobles = numeros.map(x => x * 2);

console.log(numeros);
console.log(dobles);

